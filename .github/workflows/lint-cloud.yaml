name: lint-cloud

on:
  pull_request:

permissions:
  contents: read

concurrency:
  group: lint-cloud-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-cloud:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
    - uses: actions/checkout@v7
    - uses: actions/setup-node@v6
      with:
        node-version: '22'
        cache: 'npm'
    - run: npm ci
    - run: rm -r src/00
    - run: npx abaplint .github/abaplint/abap_cloud.jsonc
